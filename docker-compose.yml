db:
  image: postgres:9.4
  ports:
    - "5432:5432"

web:
  build: .
  command: bin/rails s --port 3000 --binding 0.0.0.0
  ports:
    - "3000:3000"
  links:
    - db
  volumes:
    - .:/myapp
  env_file: .env

